__pycache__/
*.py[cod]
*$py.class
.Python
.env
.venv/
venv/
env/
# All runtime storage — user uploads, results, avatars, wardrobe, feed, and
# the SQLite DB. Never commit any of it; keep only the placeholder.
storage/*
!storage/.gitkeep
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
# Alembic local scratch DBs
*.db

# Local test / debug artifacts (not source)
*.glb
last_error.log
test_person.jpg

# PIFuHD is cloned by setup_local_gpu.sh / run_windows.bat (third-party code
# + ~1.5GB weights) — never commit it.
pifuhd/
assets/haarcascade_frontalface_default.xml
